const sides = require("./sides");

module.exports = ({ matrix, side }) => {
  switch (side) {
    case sides.EAST: {
    }
    case sides.NORTH: {
      const rowFromTopToBottom = matrix.map((row) => row[i]);
    }
    case sides.SOUTH: {
    }
    case sides.WEST: {
    }
  }
};
